services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: spacedata
      POSTGRES_PASSWORD: spacedata_dev
      POSTGRES_DB: spacedata
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
